name: Go

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Run sh
      run: |
        sudo apt install sshpass
        sshpass -p ${PASSWORD} scp -o StrictHostKeyChecking=no -r ${USERNAME}@${SERVER_IP}:/opt/minecraft_server/logs ./
      env:
        SERVER_IP: ${{secrets.SERVER_IP}}
        USERNAME: ${{secrets.USERNAME}}
        PASSWORD: ${{secrets.PASSWORD}}

    - name: Test
      run: ls -l

    - name: Set up Go
      uses: actions/setup-go@v2
      with:
        go-version: 1.17
